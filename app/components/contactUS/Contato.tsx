import Formulario from "../ContactForm/Formulario";

export default function Contato() {
    return (
        <section className="h-screen flex flex-col items-center justify-center">
            <h1>Formulário Básico</h1>
            <Formulario />
        </section>
    )
}